{
  "name": "trustiva-web",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint",
    "build:ipfs": "next build && node ../scripts/make-ipfs-dist.mjs",
    "publish": "cd .. && make publish",
    "verify": "cd .. && make verify",
    "smoke": "cd .. && make smoke"
  },
  "dependencies": {
    "next": "14.2.10",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "openpgp": "^6.0.0"
  }
}
